DATABASE_URL="postgresql://criadouro:criadouro_dev_pw@localhost:5432/criadouro_dev?schema=public"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=3333
PUBLIC_APP_URL="http://localhost:5173"
UPLOADS_DIR="uploads"

# Opcional: lembretes de medicação via WhatsApp. Sem essas variáveis, o
# lembrete apenas é registrado no log do servidor (não é enviado de verdade).
WHATSAPP_API_URL=""
WHATSAPP_API_TOKEN=""

# Segredo compartilhado para o endpoint POST /internal/lembretes/processar,
# pensado para ser chamado por um cron externo a cada minuto.
INTERNAL_CRON_SECRET="change-me"
